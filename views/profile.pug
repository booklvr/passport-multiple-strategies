extends base

block content
  main.main.main--align-middle#main-profile

   +title-1('User Profile')

   ul
    li= `firstname: ${user.firstName}`
    li= `lastname: ${user.lastName}`
    li= `email: ${user.email}`
    li= `name: ${user.name}`
    li= `name: ${user.name}`